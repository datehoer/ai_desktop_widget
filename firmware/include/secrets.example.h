#pragma once

// Optional migration fallback only. A clean device opens its built-in setup portal,
// so these values can remain empty. Never commit a real secrets.h file.
#define WIFI_SSID ""
#define WIFI_PASSWORD ""

#define STATUS_URL ""
